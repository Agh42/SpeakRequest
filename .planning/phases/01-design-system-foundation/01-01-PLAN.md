---
phase: 01-design-system-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/resources/static/chair.html
autonomous: true
requirements:
  - DS-02
  - DS-03
  - DS-04
must_haves:
  truths:
    - Chair HTML loads Tailwind CDN and no longer depends on chair-specific legacy stylesheet.
    - Required typography assets are available: Manrope for display/headline, Inter for body/labels.
    - Material Symbols Outlined renders as the chair icon system baseline.
  artifacts:
    - path: src/main/resources/static/chair.html
      provides: Tailwind bootstrap, font links, and icon system foundation.
  key_links:
    - from: src/main/resources/static/chair.html
      to: https://cdn.tailwindcss.com
      via: script include in head
      pattern: cdn.tailwindcss.com
    - from: src/main/resources/static/chair.html
      to: https://fonts.googleapis.com
      via: link tags for Manrope, Inter, and Material Symbols
      pattern: fonts.googleapis.com
---

<objective>
Bootstrap The Orchestrator design-system dependencies in the existing chair page without changing runtime behavior.

Purpose: Establish a stable visual foundation that later layout and feature migration plans can build on.
Output: Chair page head includes Tailwind CDN + typography + Material Symbols and no longer imports `styles.css`.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-design-system-foundation/01-RESEARCH.md
@src/chair-view-redesign/DESIGN.md
@src/chair-view-redesign/code.html
@src/main/resources/static/chair.html

<interfaces>
Preserve existing chair runtime contracts during visual migration:
- Do not rename or remove existing control IDs used by in-page JS (for example: `btnNext`, `btnPause`, `btnStart`, `btnReset`, `participantName`, `queue`, `speakTimer`, `meetingTimer`).
- Keep existing STOMP and DOMPurify script imports and execution flow unchanged in this phase.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace legacy chair stylesheet dependency with Tailwind bootstrap (DS-02)</name>
  <files>src/main/resources/static/chair.html</files>
  <action>In the head section, remove the `styles.css` stylesheet import for the chair page and add Tailwind CDN loading with an inline `tailwind.config` block that defines The Orchestrator token palette. Keep `stomp.min.js`, `purify.min.js`, `metadata-loader.js`, and `share.js` script imports intact.</action>
  <verify>
    <automated>powershell -NoProfile -Command "Select-String -Path 'src/main/resources/static/chair.html' -Pattern 'cdn\.tailwindcss\.com' -Quiet; if (-not $?) { exit 1 }; Select-String -Path 'src/main/resources/static/chair.html' -Pattern 'styles\.css' -Quiet; if ($?) { exit 1 }"</automated>
  </verify>
  <done>Chair page has Tailwind CDN configured and no longer imports `styles.css`.</done>
</task>

<task type="auto">
  <name>Task 2: Add typography and icon system assets (DS-03, DS-04)</name>
  <files>src/main/resources/static/chair.html</files>
  <action>Add Google Fonts links for Manrope and Inter plus Material Symbols Outlined in head. Define Tailwind font-family aliases so display/headline classes use Manrope and body/label classes use Inter.</action>
  <verify>
    <automated>powershell -NoProfile -Command "Select-String -Path 'src/main/resources/static/chair.html' -Pattern 'family=Manrope' -Quiet; if (-not $?) { exit 1 }; Select-String -Path 'src/main/resources/static/chair.html' -Pattern 'family=Inter' -Quiet; if (-not $?) { exit 1 }; Select-String -Path 'src/main/resources/static/chair.html' -Pattern 'Material\+Symbols\+Outlined' -Quiet; if (-not $?) { exit 1 }"</automated>
  </verify>
  <done>Chair page loads required font and icon assets, and Tailwind config exposes font aliases for consistent usage.</done>
</task>

</tasks>

<verification>
Run phase plan checks for this plan:
- `node .github/get-shit-done/bin/gsd-tools.cjs frontmatter validate .planning/phases/01-design-system-foundation/01-01-PLAN.md --schema plan`
- `node .github/get-shit-done/bin/gsd-tools.cjs verify plan-structure .planning/phases/01-design-system-foundation/01-01-PLAN.md`
</verification>

<success_criteria>
- Tailwind, Manrope, Inter, and Material Symbols are all loaded from `chair.html`.
- `styles.css` is not loaded by `chair.html`.
- Existing runtime JS contracts remain unchanged for phase 2+ work.
</success_criteria>

<output>
After completion, create `.planning/phases/01-design-system-foundation/01-design-system-foundation-01-SUMMARY.md`
</output>
