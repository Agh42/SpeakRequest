---
phase: 01-design-system-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/main/resources/static/chair.html
autonomous: true
requirements:
  - DS-01
  - DS-02
must_haves:
  truths:
    - Chair page uses the midnight palette and tonal surface hierarchy as the default visual language.
    - Primary sections are separated by tonal layering and spacing rather than explicit 1px divider lines.
    - Existing controls remain functional because IDs and script bindings are preserved.
  artifacts:
    - path: src/main/resources/static/chair.html
      provides: Tailwind utility-based surface styling, typography application, and section hierarchy.
  key_links:
    - from: src/main/resources/static/chair.html
      to: in-page chair script
      via: unchanged ID hooks and selectors
      pattern: getElementById
    - from: src/main/resources/static/chair.html
      to: Tailwind token classes
      via: body/wrapper/section class assignments
      pattern: bg-background|bg-surface-container|text-on-surface
---

<objective>
Apply the phase-1 visual foundation to the existing chair layout using The Orchestrator tokens and no-line sectioning.

Purpose: Deliver a consistent base visual system before structural shell and conference-table phases.
Output: Chair page surfaces, typography, and icon rendering align with DS-01 while preserving runtime behavior.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-design-system-foundation/01-RESEARCH.md
@.planning/phases/01-design-system-foundation/01-01-PLAN.md
@src/chair-view-redesign/DESIGN.md
@src/chair-view-redesign/code.html
@src/main/resources/static/chair.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply The Orchestrator base palette and typography classes (DS-01)</name>
  <files>src/main/resources/static/chair.html</files>
  <action>Update body, top-level wrapper, and major sections to use tokenized Tailwind classes (`background`, `surface*`, `on-surface*`) with Manrope/Inter usage per hierarchy. Keep semantic structure and all existing interactive elements/IDs intact.</action>
  <verify>
    <automated>powershell -NoProfile -Command "Select-String -Path 'src/main/resources/static/chair.html' -Pattern 'bg-background|bg-surface-container|text-on-surface' -Quiet; if (-not $?) { exit 1 }"</automated>
  </verify>
  <done>Core page shell and section surfaces visibly follow the new token hierarchy and typography assignments.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce no-line sectioning and remove explicit divider patterns where practical (DS-01)</name>
  <files>src/main/resources/static/chair.html</files>
  <action>Replace hard divider/border-driven grouping in chair-specific layout with tonal layer changes and spacing, following the no-line rule from the design spec. If any border remains for accessibility fallback, keep it subtle (ghost-style opacity) and document why in summary.</action>
  <verify>
    <automated>powershell -NoProfile -Command "$text = Get-Content 'src/main/resources/static/chair.html' -Raw; if ($text -match 'border:\s*1px' -or $text -match 'border-top:\s*1px' -or $text -match 'border-b\s') { exit 1 }"</automated>
  </verify>
  <done>Section hierarchy is expressed primarily through tonal containers and spacing, with no explicit 1px divider usage in redesigned chair markup.</done>
</task>

<task type="auto">
  <name>Task 3: Replace visible Font Awesome icon usage with Material Symbols baseline (DS-04)</name>
  <files>src/main/resources/static/chair.html</files>
  <action>For chair-visible controls touched in phase 1, replace Font Awesome icon elements with Material Symbols Outlined spans while keeping button IDs, labels, and behavior unchanged.</action>
  <verify>
    <automated>powershell -NoProfile -Command "Select-String -Path 'src/main/resources/static/chair.html' -Pattern 'material-symbols-outlined' -Quiet; if (-not $?) { exit 1 }; Select-String -Path 'src/main/resources/static/chair.html' -Pattern 'fa-solid|fontawesome/all.min.css' -Quiet; if ($?) { exit 1 }"</automated>
  </verify>
  <done>Chair UI no longer depends on Font Awesome and uses Material Symbols for visible control iconography.</done>
</task>

</tasks>

<verification>
Run phase plan checks for this plan:
- `node .github/get-shit-done/bin/gsd-tools.cjs frontmatter validate .planning/phases/01-design-system-foundation/01-02-PLAN.md --schema plan`
- `node .github/get-shit-done/bin/gsd-tools.cjs verify plan-structure .planning/phases/01-design-system-foundation/01-02-PLAN.md`
</verification>

<success_criteria>
- Chair page has The Orchestrator tonal look and typography baseline.
- Layout grouping is based on surfaces and spacing, not explicit 1px borders.
- Material Symbols is the active icon system for the redesigned chair page.
</success_criteria>

<output>
After completion, create `.planning/phases/01-design-system-foundation/01-design-system-foundation-02-SUMMARY.md`
</output>
