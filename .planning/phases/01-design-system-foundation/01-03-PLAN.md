---
phase: 01-design-system-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - src/main/resources/static/chair.html
autonomous: true
gap_closure: true
requirements:
  - DS-01
  - DS-02
must_haves:
  truths:
    - `participantName` and `limit` inputs render with the same tonal control system as the updated chair UI.
    - Meeting configuration controls (`configTopic`, `configParticipationFormat`, `configMeetingGoal`, `configDecisionRule`, `configDeliverable`) render with consistent The Orchestrator input/select styling.
    - Existing IDs, behavior hooks, and runtime scripts remain unchanged.
  artifacts:
    - path: src/main/resources/static/chair.html
      provides: Unified tonal form control styling for chair form inputs and dropdowns.
  key_links:
    - from: src/main/resources/static/chair.html
      to: in-page chair script
      via: unchanged IDs and selectors
      pattern: getElementById
    - from: src/main/resources/static/chair.html
      to: form controls
      via: class/style updates on existing IDs
      pattern: participantName|limit|configTopic|configParticipationFormat|configMeetingGoal|configDecisionRule|configDeliverable
---

<objective>
Close phase-1 visual gaps by aligning all key chair form controls with The Orchestrator tonal styling baseline.

Purpose: Resolve human UAT issues so the design-system foundation can be approved without carrying legacy-looking controls.
Output: Input/select controls listed in HUMAN-UAT render in the same tonal/typographic language as the rest of phase-1 UI.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-design-system-foundation/01-HUMAN-UAT.md
@.planning/phases/01-design-system-foundation/01-VERIFICATION.md
@.planning/phases/01-design-system-foundation/01-01-PLAN.md
@.planning/phases/01-design-system-foundation/01-02-PLAN.md
@src/main/resources/static/chair.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply unified tonal input styling to participant and limit controls</name>
  <files>src/main/resources/static/chair.html</files>
  <action>Update `participantName` and `limit` control styling so both use the same tonal visual treatment (surface container fill, on-surface text color, rounded corners, and clear focus state) already used in the migrated chair sections.</action>
  <verify>
    <automated>powershell -NoProfile -Command "$text = Get-Content 'src/main/resources/static/chair.html' -Raw; if ($text -match 'id=\"participantName\"[^>]*(bg-surface-container|bg-surface-container-high)' -and $text -match 'id=\"limit\"[^>]*(bg-surface-container|bg-surface-container-high)') { 'PASS' } else { exit 1 }"</automated>
  </verify>
  <done>`participantName` and `limit` no longer appear as legacy/default controls and match the tonal form style.</done>
</task>

<task type="auto">
  <name>Task 2: Align meeting configuration text/select controls with design tokens</name>
  <files>src/main/resources/static/chair.html</files>
  <action>Apply consistent token-based styling to `configTopic`, `configParticipationFormat`, `configMeetingGoal`, `configDecisionRule`, and `configDeliverable` so these controls share the same visual language as the updated chair shell.</action>
  <verify>
    <automated>powershell -NoProfile -Command "$ids = 'configTopic','configParticipationFormat','configMeetingGoal','configDecisionRule','configDeliverable'; $text = Get-Content 'src/main/resources/static/chair.html' -Raw; foreach ($id in $ids) { if ($text -notmatch ('id=\"' + $id + '\"[^>]*(bg-surface-container|bg-surface-container-high)')) { exit 1 } }; 'PASS'"</automated>
  </verify>
  <done>All listed meeting configuration controls render with consistent tonal select/input styling.</done>
</task>

<task type="auto">
  <name>Task 3: Preserve existing runtime behavior and IDs while closing UAT gap</name>
  <files>src/main/resources/static/chair.html</files>
  <action>Confirm all existing IDs and script hooks remain intact after styling updates, then run a final grep-based integrity check for key control IDs.</action>
  <verify>
    <automated>powershell -NoProfile -Command "$ids = 'btnNext','btnPause','btnStart','btnReset','participantName','queue','speakTimer','meetingTimer','configTopic','configParticipationFormat','configMeetingGoal','configDecisionRule','configDeliverable'; $text = Get-Content 'src/main/resources/static/chair.html' -Raw; foreach ($id in $ids) { if ($text -notmatch ('id=\"' + $id + '\"')) { exit 1 } }; 'PASS'"</automated>
  </verify>
  <done>Gap closure styling changes are isolated to presentation; runtime hooks remain unchanged.</done>
</task>

</tasks>

<verification>
Run plan checks:
- `node .github/get-shit-done/bin/gsd-tools.cjs frontmatter validate .planning/phases/01-design-system-foundation/01-03-PLAN.md --schema plan`
- `node .github/get-shit-done/bin/gsd-tools.cjs verify plan-structure .planning/phases/01-design-system-foundation/01-03-PLAN.md`
</verification>

<success_criteria>
- UAT-reported controls use the same tonal form styling baseline.
- No control IDs are changed or removed.
- Plan is marked as gap closure and ready for `/gsd-execute-phase 1 --gaps-only`.
</success_criteria>

<output>
After completion, create `.planning/phases/01-design-system-foundation/01-03-SUMMARY.md`
</output>
