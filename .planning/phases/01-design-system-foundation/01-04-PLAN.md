---
phase: 01-design-system-foundation
plan: 04
type: execute
wave: 4
depends_on:
  - 01-03
files_modified:
  - src/main/resources/static/chair.html
autonomous: true
gap_closure: true
requirements:
  - DS-01
must_haves:
  truths:
    - All chair-view text inputs use the same The Orchestrator tonal input styling baseline.
    - Poll creation text fields (including `pollQuestion`) are no longer legacy-looking.
    - Existing input IDs and JS behavior remain unchanged.
  artifacts:
    - path: src/main/resources/static/chair.html
      provides: Consistent class-based styling on all text input controls.
  key_links:
    - from: src/main/resources/static/chair.html
      to: poll creation UX
      via: styled poll input fields
      pattern: pollQuestion
    - from: src/main/resources/static/chair.html
      to: runtime scripts
      via: unchanged IDs
      pattern: getElementById
---

<objective>
Close the remaining phase-1 form-styling gap by ensuring every text input in chair view uses the same tonal styling pattern.

Purpose: Remove the last visible legacy input appearance (reported at `pollQuestion`) and enforce a consistent input baseline across the page.
Output: `pollQuestion` and any other text inputs adopt the same tokenized input classes as `participantName` and `configTopic`.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-design-system-foundation/01-HUMAN-UAT.md
@.planning/phases/01-design-system-foundation/01-VERIFICATION.md
@.planning/phases/01-design-system-foundation/01-03-PLAN.md
@src/main/resources/static/chair.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inventory all chair-view text inputs and identify unstyled fields</name>
  <files>src/main/resources/static/chair.html</files>
  <action>Audit every `<input type="text">` or text-like input in chair view and identify controls that do not yet use the tokenized tonal input class pattern.</action>
  <verify>
    <automated>powershell -NoProfile -Command "Select-String -Path 'src/main/resources/static/chair.html' -Pattern '<input[^>]*(id=\"[^\"]+\")[^>]*>' | Measure-Object | ForEach-Object { if ($_.Count -gt 0) { 'PASS' } else { exit 1 } }"</automated>
  </verify>
  <done>All text input controls are enumerated with target styling status known before edits.</done>
</task>

<task type="auto">
  <name>Task 2: Apply tonal input classes to every remaining unstyled text input</name>
  <files>src/main/resources/static/chair.html</files>
  <action>Apply the same class baseline used by `participantName`/`configTopic` to `pollQuestion` and any other remaining unstyled text inputs, preserving IDs and existing attributes.</action>
  <verify>
    <automated>powershell -NoProfile -Command "$text = Get-Content 'src/main/resources/static/chair.html' -Raw; if ($text -match 'id=\"pollQuestion\"[^>]*(bg-surface-container|bg-surface-container-high)') { 'PASS' } else { exit 1 }"</automated>
  </verify>
  <done>All remaining text inputs, including poll creation fields, follow the tonal input class baseline.</done>
</task>

<task type="auto">
  <name>Task 3: Guard runtime contract integrity for updated inputs</name>
  <files>src/main/resources/static/chair.html</files>
  <action>Confirm that no input IDs used by scripts were renamed or removed while applying visual classes.</action>
  <verify>
    <automated>powershell -NoProfile -Command "$ids = 'participantName','configTopic','pollQuestion','multiselectPollQuestion'; $text = Get-Content 'src/main/resources/static/chair.html' -Raw; foreach ($id in $ids) { if ($text -notmatch ('id=\"' + $id + '\"')) { exit 1 } }; 'PASS'"</automated>
  </verify>
  <done>Gap closure remains presentation-only; script targets are intact.</done>
</task>

</tasks>

<verification>
Run plan checks:
- `node .github/get-shit-done/bin/gsd-tools.cjs frontmatter validate .planning/phases/01-design-system-foundation/01-04-PLAN.md --schema plan`
- `node .github/get-shit-done/bin/gsd-tools.cjs verify plan-structure .planning/phases/01-design-system-foundation/01-04-PLAN.md`
</verification>

<success_criteria>
- `pollQuestion` and all remaining text inputs use tonal input classes.
- No input IDs are changed.
- Plan is marked with `gap_closure: true` and ready for `/gsd-execute-phase 1 --gaps-only`.
</success_criteria>

<output>
After completion, create `.planning/phases/01-design-system-foundation/01-04-SUMMARY.md`
</output>
