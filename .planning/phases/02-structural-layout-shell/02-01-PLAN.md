---
phase: 02-structural-layout-shell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/resources/static/chair.html
autonomous: true
requirements:
  - LAYOUT-01
  - LAYOUT-03
must_haves:
  truths:
    - Desktop chair view has a fixed top header with room/share actions, connection/chair status, settings entry, and desktop-only destroy-room action.
    - Desktop chair view renders a three-column shell with left utility panels, a center foundation area, and right queue column while preserving current functionality.
    - Existing chair controls and metadata remain usable after the shell restructuring.
  artifacts:
    - path: src/main/resources/static/chair.html
      provides: Fixed header shell and desktop 12-grid page structure with anchored sections.
  key_links:
    - from: src/main/resources/static/chair.html
      to: roomCodeDisplay
      via: Header room badge text binding
      pattern: id="roomCodeDisplay"
    - from: src/main/resources/static/chair.html
      to: updateUI(state)
      via: Existing DOM update flow preserved after layout move
      pattern: function updateUI\(state\)
---

<objective>
Build the structural desktop shell for chair view: fixed top header and three-column layout foundation, while preserving all current runtime behavior.

Purpose: Establish a stable frame and anchor contract for later sidebar/mobile navigation and table/control migration work.
Output: `chair.html` contains the fixed-header desktop shell and anchored section layout with existing controls repositioned, not replaced.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-structural-layout-shell/02-CONTEXT.md
@src/chair-view-redesign/DESIGN.md
@src/chair-view-redesign/code.html
@src/main/resources/static/chair.html

<interfaces>
Existing contracts that must remain intact:
- Room and status bindings: `roomCodeDisplay`, `roomCodeLarge`, `conn`, `chairStatus`.
- Core control IDs/events: `btnNext`, `btnPause`, `btnStart`, `btnReset`, `btnSetLimit`, `btnDestroyRoom`, `btnPopout`.
- Queue/timer/state IDs: `queue`, `qCount`, `curName`, `speakTimer`, `meetingTimer`, `remaining`.
- Runtime wiring entry points: `connect()`, `updateUI(state)`, `updatePollUI(state)`, `updateRoomConfigUI(state)`.

New shell anchors to define in this plan for downstream nav behavior:
- `section-controls`, `section-center-shell`, `section-queue`, `section-poll`, `section-menu`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shell anchor contracts and semantic section wrappers (per D-04, D-08, D-10)</name>
  <files>src/main/resources/static/chair.html</files>
  <action>Refactor the main layout markup to define stable anchored section containers (`section-controls`, `section-center-shell`, `section-queue`, `section-poll`, `section-menu`) while preserving all existing internal control IDs and script hooks. Do not introduce new routes or view switching; keep a single-page anchor model only.</action>
  <verify>
    <automated>powershell -NoProfile -Command "rg -n 'id=\"section-controls\"|id=\"section-center-shell\"|id=\"section-queue\"|id=\"section-poll\"|id=\"section-menu\"' src/main/resources/static/chair.html; if ($LASTEXITCODE -ne 0) { exit 1 }; rg -n 'id=\"btnNext\"|id=\"btnDestroyRoom\"|id=\"queue\"|id=\"speakTimer\"' src/main/resources/static/chair.html; if ($LASTEXITCODE -ne 0) { exit 1 }"</automated>
  </verify>
  <done>All shell anchor sections exist and legacy runtime IDs are still present in `chair.html`.</done>
</task>

<task type="auto">
  <name>Task 2: Implement fixed top header action model for desktop/mobile (per D-01, D-02, D-03)</name>
  <files>src/main/resources/static/chair.html</files>
  <action>Replace the current header structure with a fixed top bar aligned to The Orchestrator shell: include room badge with copy/share affordances, compact connection/chair status indicators, settings entry point, and a desktop-only destroy action. Keep mobile header compact and move destructive/settings secondary actions into menu wiring stubs for Plan 02.</action>
  <verify>
    <automated>powershell -NoProfile -Command "rg -n 'class=\"[^\"]*fixed[^\"]*top-0|id=\"btnShareParticipantUrlHeader\"|id=\"conn\"|id=\"chairStatus\"|id=\"btnDestroyRoom\"' src/main/resources/static/chair.html; if ($LASTEXITCODE -ne 0) { exit 1 }"</automated>
  </verify>
  <done>Header is fixed and exposes the required room/share/status controls with desktop-only destroy affordance.</done>
</task>

<task type="auto">
  <name>Task 3: Implement desktop 12-grid three-column shell with center foundation area (per D-08, D-09, D-10, D-11)</name>
  <files>src/main/resources/static/chair.html</files>
  <action>Apply desktop shell layout as left utility, center foundation, right queue columns (4/5/3) using anchored sections and natural page scrolling (only header fixed). Reposition existing live controls/poll/share/metadata into the new shell instead of placeholders, and keep room metadata visible in the left utility/menu area.</action>
  <verify>
    <automated>powershell -NoProfile -Command "rg -n 'lg:grid-cols-12|lg:col-span-4|lg:col-span-5|lg:col-span-3|id=\"section-center-shell\"|id=\"configDescriptions\"' src/main/resources/static/chair.html; if ($LASTEXITCODE -ne 0) { exit 1 }"</automated>
  </verify>
  <done>Desktop shell renders as a three-column 12-grid foundation with existing functional sections preserved in their new positions.</done>
</task>

</tasks>

<verification>
- `node .github/get-shit-done/bin/gsd-tools.cjs frontmatter validate .planning/phases/02-structural-layout-shell/02-01-PLAN.md --schema plan`
- `node .github/get-shit-done/bin/gsd-tools.cjs verify plan-structure .planning/phases/02-structural-layout-shell/02-01-PLAN.md`
</verification>

<success_criteria>
- LAYOUT-01 is implemented by fixed header structure with required action/status elements.
- LAYOUT-03 shell structure exists with 4/5/3 desktop columns and center foundation area.
- Existing runtime chair controls continue to be addressable by their prior IDs.
</success_criteria>

<output>
After completion, create `.planning/phases/02-structural-layout-shell/02-structural-layout-shell-01-SUMMARY.md`
</output>
