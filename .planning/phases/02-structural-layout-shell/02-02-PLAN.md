---
phase: 02-structural-layout-shell
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/main/resources/static/chair.html
autonomous: true
requirements:
  - LAYOUT-02
  - LAYOUT-04
must_haves:
  truths:
    - Desktop sidebar defaults to collapsed icon rail and can expand for labeled anchor navigation.
    - Mobile experience uses bottom tabs to scroll to Controls, Queue, Poll, and Menu sections with active-tab highlighting based on visible section.
    - Mobile Menu opens a slide-over panel containing secondary room actions, settings access, and room metadata without creating new routes.
  artifacts:
    - path: src/main/resources/static/chair.html
      provides: Collapsible sidebar, mobile bottom-nav bar, slide-over menu, and active-section navigation script.
  key_links:
    - from: src/main/resources/static/chair.html
      to: section-controls/section-queue/section-poll/section-menu
      via: Sidebar + mobile tab anchor targets
      pattern: href="#section-
    - from: src/main/resources/static/chair.html
      to: Active nav highlighting logic
      via: Scroll observer or equivalent section-detection handler
      pattern: active
---

<objective>
Add navigation behaviors on top of the shell: collapsible desktop sidebar, mobile bottom tabs, and slide-over menu with active-section sync.

Purpose: Complete Phase 2 interaction scaffolding so the redesigned shell is usable across desktop and mobile before feature migration phases.
Output: `chair.html` contains sidebar/mobile navigation mechanics that operate within one page and preserve existing backend/event contracts.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-structural-layout-shell/02-CONTEXT.md
@.planning/phases/02-structural-layout-shell/02-01-PLAN.md
@src/chair-view-redesign/code.html
@src/main/resources/static/chair.html

<interfaces>
Anchors and shell contracts expected from Plan 02-01:
- `section-controls`, `section-center-shell`, `section-queue`, `section-poll`, `section-menu`.
- Existing runtime controls and status IDs remain unchanged.

Navigation behavior contracts to implement in this plan:
- Sidebar links and mobile tabs target in-page anchors only.
- Active-state highlighting updates from scroll position (not just click state).
- Agenda item remains hidden (do not ship a placeholder destination) per D-07.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build collapsible desktop sidebar with icon-rail default and anchor navigation (per D-04, D-05, D-07)</name>
  <files>src/main/resources/static/chair.html</files>
  <action>Implement a desktop sidebar that is collapsed by default to icon rail width and can expand/collapse via header/sidebar toggles. Sidebar items must navigate via in-page anchor scroll to real sections (Controls, Queue, Poll), use active-state styling, and keep Agenda hidden entirely until scoped in a later phase.</action>
  <verify>
    <automated>powershell -NoProfile -Command "rg -n 'id=\"sidebar\"|collapsed|href=\"#section-controls\"|href=\"#section-queue\"|href=\"#section-poll\"' src/main/resources/static/chair.html; if ($LASTEXITCODE -ne 0) { exit 1 }; rg -n 'Agenda|section-agenda' src/main/resources/static/chair.html; if ($LASTEXITCODE -eq 0) { exit 1 }"</automated>
  </verify>
  <done>Sidebar supports collapsed/expanded modes and anchors to real sections, with no Agenda placeholder shipped.</done>
</task>

<task type="auto">
  <name>Task 2: Add mobile bottom navigation and menu slide-over model (per D-06, D-12, D-13, D-14)</name>
  <files>src/main/resources/static/chair.html</files>
  <action>Add mobile bottom tabs for Controls, Queue, Poll, and Menu that target in-page section anchors. Implement a left slide-over menu opened from menu affordances, and place secondary room actions/settings/metadata content there for mobile while keeping conference-table center content de-emphasized.</action>
  <verify>
    <automated>powershell -NoProfile -Command "rg -n 'id=\"mobileBottomNav\"|Controls|Queue|Poll|Menu|id=\"mobileMenuPanel\"|translateX\(-100%\)|#section-menu' src/main/resources/static/chair.html; if ($LASTEXITCODE -ne 0) { exit 1 }"</automated>
  </verify>
  <done>Mobile bottom nav and slide-over menu exist and map to anchored sections with room/menu content in the mobile menu flow.</done>
</task>

<task type="auto">
  <name>Task 3: Implement active-section synchronization for sidebar and mobile tabs (per D-12, D-15)</name>
  <files>src/main/resources/static/chair.html</files>
  <action>Add JavaScript that synchronizes active states for desktop sidebar items and mobile tabs based on current visible section while scrolling, with smooth anchor-scroll behavior and offset handling for fixed header. Avoid route changes, backend changes, and event-contract changes.</action>
  <verify>
    <automated>powershell -NoProfile -Command "rg -n 'IntersectionObserver|getBoundingClientRect|scrollIntoView|active|section-controls|section-queue|section-poll|section-menu' src/main/resources/static/chair.html; if ($LASTEXITCODE -ne 0) { exit 1 }"</automated>
  </verify>
  <done>Sidebar and mobile tabs auto-highlight the visible section and keep click/scroll states aligned.</done>
</task>

</tasks>

<verification>
- `node .github/get-shit-done/bin/gsd-tools.cjs frontmatter validate .planning/phases/02-structural-layout-shell/02-02-PLAN.md --schema plan`
- `node .github/get-shit-done/bin/gsd-tools.cjs verify plan-structure .planning/phases/02-structural-layout-shell/02-02-PLAN.md`
</verification>

<success_criteria>
- LAYOUT-02 sidebar behavior is fully represented in desktop and mobile shell navigation.
- LAYOUT-04 mobile bottom-nav anchor flow works with active-state updates from scroll position.
- No backend APIs, routes, or STOMP contracts are introduced or modified.
</success_criteria>

<output>
After completion, create `.planning/phases/02-structural-layout-shell/02-structural-layout-shell-02-SUMMARY.md`
</output>
