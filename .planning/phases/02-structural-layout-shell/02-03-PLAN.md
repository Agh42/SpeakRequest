---
phase: 02-structural-layout-shell
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - src/main/resources/static/chair.html
autonomous: true
requirements:
  - LAYOUT-02
  - LAYOUT-04
gap_closure: true
must_haves:
  truths:
    - On mobile, tapping Menu reveals the slide-over panel itself (not just a dimmed backdrop), and closing it restores the main view state.
    - While scrolling, exactly one section is treated as active for nav highlighting so desktop and mobile indicators do not remain simultaneously sticky.
  artifacts:
    - path: src/main/resources/static/chair.html
      provides: Fixed mobile menu open/close panel positioning behavior and deterministic single-active-section navigation state logic.
  key_links:
    - from: src/main/resources/static/chair.html
      to: "#mobileMenuPanel"
      via: open/close handlers + data-open driven CSS transform
      pattern: mobileMenuPanel|data-open|transform
    - from: src/main/resources/static/chair.html
      to: sidebar/mobile nav active classes
      via: full-section-state active computation
      pattern: IntersectionObserver|active|classList
---

<objective>
Close the two diagnosed Phase 02 shell regressions: mobile Menu panel remains off-screen and nav active-state highlighting is unstable while scrolling.

Purpose: Restore the intended shell usability from Phase 02 without expanding scope into conference-table or functional-control migration work.
Output: `chair.html` applies deterministic section activation and a working mobile slide-over visibility flow.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-structural-layout-shell/02-UAT.md
@.planning/phases/02-structural-layout-shell/02-02-PLAN.md
@src/main/resources/static/chair.html

<interfaces>
Relevant existing shell contracts in `chair.html`:
- `#mobileMenuBackdrop`, `#mobileMenuPanel`, and menu open/close handlers manage the mobile slide-over flow.
- Sidebar/mobile links target `#section-controls`, `#section-queue`, `#section-poll`, and `#section-menu`.
- Active-state sync is currently driven by section visibility logic and class toggling for nav items.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix mobile menu panel visibility regression so panel enters viewport on open (per UAT gap test 5)</name>
  <files>src/main/resources/static/chair.html</files>
  <behavior>
    - Test 1: Opening mobile Menu changes panel from hidden/off-canvas to visible in-viewport state.
    - Test 2: Closing mobile Menu returns panel to hidden/off-canvas state while preserving page state.
  </behavior>
  <action>Update the mobile slide-over implementation so `#mobileMenuPanel` positioning is controlled consistently by the open/close state instead of a permanently overriding inline transform. Keep the existing overlay/backdrop behavior, and do not alter backend wiring or route behavior.</action>
  <verify>
    <automated>powershell -NoProfile -Command "$ErrorActionPreference='Stop'; $job = Start-Job -ScriptBlock { Set-Location 'O:/akoderman/git/SpeakRequest'; ./gradlew.bat bootRun }; try { $up=$false; for ($i=0; $i -lt 90; $i++) { try { Invoke-WebRequest 'http://127.0.0.1:8080/chair.html' -UseBasicParsing | Out-Null; $up=$true; break } catch { Start-Sleep -Seconds 1 } }; if (-not $up) { throw 'App did not start within timeout' }; npx -y playwright@1.53.1 eval "(async ({ page }) => { await page.goto('http://127.0.0.1:8080/chair.html'); await page.setViewportSize({ width: 390, height: 844 }); await page.locator('#section-queue').scrollIntoViewIfNeeded(); await page.waitForTimeout(150); const scrollBefore = await page.evaluate(() => window.scrollY); await page.getByRole('button', { name: /menu/i }).click(); const panel = page.locator('#mobileMenuPanel'); await panel.waitFor({ state: 'visible' }); const openBox = await panel.boundingBox(); if (!openBox || openBox.x + openBox.width <= 0) throw new Error('Panel still off-canvas after open'); await page.keyboard.press('Escape'); await page.waitForTimeout(200); const closedBox = await panel.boundingBox(); if (closedBox && closedBox.x < 200) throw new Error('Panel did not return off-canvas on close'); const scrollAfter = await page.evaluate(() => window.scrollY); if (Math.abs(scrollAfter - scrollBefore) > 8) throw new Error('Page scroll position changed across menu open/close (state loss)'); })()" } finally { Stop-Job $job -ErrorAction SilentlyContinue; Remove-Job $job -Force -ErrorAction SilentlyContinue }"</automated>
  </verify>
  <done>Menu open reveals the panel itself (not backdrop-only), and close returns it off-canvas without losing local shell state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace callback-entry-based section selection with deterministic single-active computation (per UAT gap test 6)</name>
  <files>src/main/resources/static/chair.html</files>
  <behavior>
    - Test 1: Active section is selected from all tracked sections using stable ranking, not only callback delta entries.
    - Test 2: At any point, nav highlight classes resolve to one active target for both desktop and mobile controls.
  </behavior>
  <action>Refactor the section visibility tracking logic to maintain full section state and compute the active section deterministically on each update. Ensure class application first clears old active markers and then applies exactly one active target across sidebar and mobile nav items to eliminate sticky dual-highlights.</action>
  <verify>
    <automated>powershell -NoProfile -Command "$ErrorActionPreference='Stop'; $job = Start-Job -ScriptBlock { Set-Location 'O:/akoderman/git/SpeakRequest'; ./gradlew.bat bootRun }; try { $up=$false; for ($i=0; $i -lt 90; $i++) { try { Invoke-WebRequest 'http://127.0.0.1:8080/chair.html' -UseBasicParsing | Out-Null; $up=$true; break } catch { Start-Sleep -Seconds 1 } }; if (-not $up) { throw 'App did not start within timeout' }; npx -y playwright@1.53.1 eval "(async ({ page }) => { await page.goto('http://127.0.0.1:8080/chair.html'); await page.setViewportSize({ width: 1366, height: 900 }); const sectionIds = ['section-controls','section-queue','section-poll','section-menu']; const readTarget = async (root) => page.locator(root + ' .is-active, ' + root + ' [aria-current=\"page\"]').first().evaluate((el) => { const href = el.getAttribute('href') || ''; const dataTarget = el.getAttribute('data-target') || el.getAttribute('data-section') || ''; const fromHref = href.startsWith('#') ? href.slice(1) : ''; return dataTarget || fromHref || ''; }); for (const id of sectionIds) { await page.locator('#' + id).scrollIntoViewIfNeeded(); await page.waitForTimeout(220); const activeDesktop = await page.locator('#desktopSidebar .is-active, #desktopSidebar [aria-current=\"page\"]').count(); const activeMobile = await page.locator('#mobileBottomNav .is-active, #mobileBottomNav [aria-current=\"page\"]').count(); if (activeDesktop !== 1) throw new Error('Expected exactly one desktop active target while on ' + id + ', got ' + activeDesktop); if (activeMobile !== 1) throw new Error('Expected exactly one mobile active target while on ' + id + ', got ' + activeMobile); const desktopTarget = await readTarget('#desktopSidebar'); const mobileTarget = await readTarget('#mobileBottomNav'); if (desktopTarget !== id) throw new Error('Desktop active target mismatch for ' + id + ': got ' + desktopTarget); if (mobileTarget !== id) throw new Error('Mobile active target mismatch for ' + id + ': got ' + mobileTarget); } })()" } finally { Stop-Job $job -ErrorAction SilentlyContinue; Remove-Job $job -Force -ErrorAction SilentlyContinue }"</automated>
  </verify>
  <done>Scrolling produces stable, single-target nav highlighting with no lingering simultaneous active states.</done>
</task>

</tasks>

<verification>
- `node .github/get-shit-done/bin/gsd-tools.cjs frontmatter validate .planning/phases/02-structural-layout-shell/02-03-PLAN.md --schema plan`
- `node .github/get-shit-done/bin/gsd-tools.cjs verify plan-structure .planning/phases/02-structural-layout-shell/02-03-PLAN.md`
</verification>

<success_criteria>
- UAT test 5 truth is restored: Menu opens the panel into view and closes cleanly without state loss.
- UAT test 6 truth is restored: active-section sync is deterministic with exactly one active nav target at a time.
- Scope remains Phase 02 shell-only: no new routes, no backend API changes, no STOMP contract changes.
</success_criteria>

<output>
After completion, create `.planning/phases/02-structural-layout-shell/02-structural-layout-shell-03-SUMMARY.md`
</output>