---
phase: 03-conference-table-experience
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/resources/static/chair.html
autonomous: true
requirements:
  - TABLE-01
  - TABLE-05
  - TABLE-06
must_haves:
  truths:
    - "The chair sees a round conference table in the center column on desktop."
    - "The table center keeps the active speaker timer as the primary read with supporting room metadata."
    - "The conference table stays hidden on mobile."
  artifacts:
    - path: src/main/resources/static/chair.html
      provides: "Desktop conference-table scaffold and responsive visibility rules"
    - path: src/main/resources/static/chair.html
      provides: "Preserved timer and room-code DOM bindings for the center treatment"
  key_links:
    - from: "src/main/resources/static/chair.html#section-center-shell"
      to: "desktop conference table"
      via: "reworked center-column markup and responsive classes"
      pattern: "section-center-shell|conference-table|table-ring"
    - from: "desktop conference table"
      to: "existing timer bindings"
      via: "kept DOM IDs for the current render path"
      pattern: "speakTimer|remaining|meetingTimer|roomCodeDisplay2|curName"
---

<objective>
Create the desktop conference-table scaffold for Phase 3 so the chair view gains a central round-table centerpiece without disturbing the phase-2 shell.

Purpose: The center column must become the visual focal point for desktop while mobile continues to rely on the shell navigation.
Output: Updated chair markup with the table container, center timing stack, and mobile-hidden behavior in place.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-conference-table-experience/03-CONTEXT.md

<interfaces>
From `src/main/resources/static/chair.html`:
```js
const elQueue = $('#queue');
const elQCount = $('#qCount');
const elCurName = $('#curName');
const elSpeakTimer = $('#speakTimer');
const elRemaining = $('#remaining');
const elMeeting = $('#meetingTimer');
const elLimit = $('#limit');

function updateUI(state) {
  // queue, current speaker, timers, poll, room config
}

function updateTimers(state) {
  // meeting elapsed and speaker remaining time
}
```

State already consumed by the chair view:
- `state.queue`
- `state.current`
- `state.meetingStartSec`
- `state.defaultLimitSec`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild the center column into the conference-table scaffold</name>
  <files>src/main/resources/static/chair.html</files>
  <action>Replace the `section-center-shell` placeholder with the desktop round-table composition per D-01 and TABLE-01. Add the table ring, orbit seat containers, and the center timing stack while preserving the existing DOM IDs that later tasks depend on (`speakTimer`, `remaining`, `meetingTimer`, `roomCodeDisplay2`, `curName`). Keep the shell structure single-page and do not introduce any backend, route, or STOMP changes.</action>
  <verify>pwsh -NoProfile -Command "Select-String -Path src/main/resources/static/chair.html -Pattern 'section-center-shell|conference-table|table-ring|speakTimer|roomCodeDisplay2|curName'"</verify>
  <done>The center column contains the new table scaffold, and the existing timer and room metadata bindings are still present.</done>
</task>

<task type="auto">
  <name>Task 2: Add responsive visibility and tonal styling for the desktop-only table</name>
  <files>src/main/resources/static/chair.html</files>
  <action>Implement the table-specific styling in the existing chair markup so the centerpiece uses Orchestrator tonal surfaces, no explicit 1px section borders, and desktop-only visibility per D-02 and TABLE-06. Keep the phase-2 shell sections as the mobile path and ensure the table is suppressed below the desktop breakpoint without changing the surrounding navigation contract.</action>
  <verify>pwsh -NoProfile -Command "Select-String -Path src/main/resources/static/chair.html -Pattern 'md:hidden|lg:col-span-5|lg:block|surface-container|surface-container-high|border-white/5'"</verify>
  <done>The table is visually isolated to the desktop center column and remains hidden on mobile while the shell navigation stays intact.</done>
</task>

</tasks>

<verification>
Confirm the center column is now a desktop-only table scaffold and that the preserved timer IDs still exist for later rendering work.
</verification>

<success_criteria>
The center of the chair page is structurally ready for the conference-table experience, and the mobile layout still uses the phase-2 shell instead of the table.
</success_criteria>

<output>
After completion, create `.planning/phases/03-conference-table-experience/03-01-SUMMARY.md`
</output>