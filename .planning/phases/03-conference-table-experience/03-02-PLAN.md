---
phase: 03-conference-table-experience
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/main/resources/static/chair.html
autonomous: true
requirements:
  - TABLE-02
  - TABLE-03
  - TABLE-04
must_haves:
  truths:
    - "Each participant appears as a monogram avatar with a stable participant color."
    - "The current speaker is visibly highlighted and queued participants show numbered position badges."
    - "The table center keeps the current speaker countdown prominent while room code and secondary timing details remain readable but not dominant."
  artifacts:
    - path: src/main/resources/static/chair.html
      provides: "Participant-seat rendering helpers and status badge markup"
    - path: src/main/resources/static/chair.html
      provides: "Stable avatar color and initials derivation from the existing state"
  key_links:
    - from: "updateUI(state)"
      to: "participant seat rendering"
      via: "derive seats from current state.queue and state.current"
      pattern: "updateUI\(state\)|state\.queue|state\.current"
    - from: "participant rendering"
      to: "status treatment"
      via: "apply speaking ring and queue position badges"
      pattern: "monogram|avatar|badge|speaking|queue"
---

<objective>
Wire the live room state into the conference-table centerpiece so the chair can read participant identity, speaking status, and queue order at a glance.

Purpose: The table must be operational, not decorative, and it must derive its state from the existing snapshot without changing backend events.
Output: Updated chair rendering logic that draws monogram avatars, speaking emphasis, queue badges, and center timing hierarchy from the current state.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-conference-table-experience/03-CONTEXT.md

<interfaces>
From `src/main/resources/static/chair.html`:
```js
function updateUI(state) {
  currentState = state;
  elQCount.textContent = state.queue.length;
  state.queue.forEach((p, i) => { /* queue render path */ });
  if (state.current) {
    elCurName.textContent = DOMPurify.sanitize(state.current.entry.name);
  }
  updateTimers(state);
}

function updateTimers(state) {
  const c = state.current;
  // elapsedMs, running, startedAtSec, limitSec
}
```

Relevant state fields already available:
- `state.queue[i].name`
- `state.current.entry.name`
- `state.current.elapsedMs`
- `state.current.running`
- `state.current.startedAtSec`
- `state.current.limitSec`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Derive stable participant avatar data from the existing state snapshot</name>
  <files>src/main/resources/static/chair.html</files>
  <action>Extend `updateUI(state)` so the centerpiece can derive participant initials and a stable per-participant color from the existing room state per D-03. Keep DOMPurify in the name-rendering path, compute seat data from `state.current` plus `state.queue`, and avoid any new backend fields or topic changes.</action>
  <verify>pwsh -NoProfile -Command "Select-String -Path src/main/resources/static/chair.html -Pattern 'DOMPurify\.sanitize|state\.queue|state\.current\.entry\.name|stable|initials|avatar'"</verify>
  <done>The render path can build monogram avatars and stable colors from the current room snapshot without changing STOMP payloads.</done>
</task>

<task type="auto">
  <name>Task 2: Apply speaking highlight, queue position badges, and center hierarchy</name>
  <files>src/main/resources/static/chair.html</files>
  <action>Render the current speaker with the tertiary highlight treatment and add numbered queue-position badges for waiting participants per D-04. Keep the center stack aligned with D-05 through D-07: the current speaker countdown remains the primary read, room code is secondary rather than focal, and the smaller timing metadata stays visible inside the centerpiece.</action>
  <verify>pwsh -NoProfile -Command "Select-String -Path src/main/resources/static/chair.html -Pattern 'tertiary|queue position|badge|speaker countdown|room code|meeting timer|remaining'"</verify>
  <done>The table clearly distinguishes the active speaker, queued participants, and the center timing hierarchy using only the existing room snapshot.</done>
</task>

</tasks>

<verification>
Confirm the rendering path now produces monogram avatars, speaking emphasis, and queue badges from the existing room state while preserving the current timer and sanitization behavior.
</verification>

<success_criteria>
The conference table reads as an operational live status surface: identity, speaking state, queue order, and timing are all visible without any backend changes.
</success_criteria>

<output>
After completion, create `.planning/phases/03-conference-table-experience/03-02-SUMMARY.md`
</output>