---
phase: 04-functional-control-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/resources/static/chair.html
autonomous: true
requirements:
  - CTRL-01
  - CTRL-02
  - CTRL-03
  - CTRL-04
must_haves:
  truths:
    - "The chair can run the existing speak-request, withdraw, per-speaker limit, and timer actions from the migrated Meeting Controls card."
    - "The Speaker Queue reads as an operational queue with the current speaker separated from queued speakers and a visible Force Add Speaker action."
    - "The existing popout utility remains attached to the chair control surface and the current STOMP destinations stay unchanged."
  artifacts:
    - path: src/main/resources/static/chair.html
      provides: "Meeting Controls card with preserved control IDs and existing timer/request handlers"
    - path: src/main/resources/static/chair.html
      provides: "Speaker Queue card with pinned current-speaker treatment and Force Add Speaker action"
    - path: src/main/resources/static/chair.html
      provides: "Existing popout utility and chair-only actions retained inside the control surface"
  key_links:
    - from: "src/main/resources/static/chair.html"
      to: "/app/room/{roomCode}/request"
      via: "existing chair proxy request handler"
      pattern: "btnParticipantRequest|btnParticipantWithdraw|participantName"
    - from: "src/main/resources/static/chair.html"
      to: "/app/room/{roomCode}/timer"
      via: "existing timer control handlers"
      pattern: "btnStart|btnPause|btnNext|btnReset"
    - from: "src/main/resources/static/chair.html"
      to: "state.queue and state.current"
      via: "queue rendering in updateUI(state)"
      pattern: "queue|qCount|curName|curInitials"
---

<objective>
Rebuild the chair's Meeting Controls and Speaker Queue surfaces inside the Phase 2 shell so the redesigned left and right operational panels still drive the meeting through the existing STOMP contract.

Purpose: The chair must be able to control speakers and queue flow from the new layout without any backend or route changes.
Output: Updated chair.html markup and wiring for the Meeting Controls and Speaker Queue cards, with the current control IDs preserved.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-functional-control-migration/04-CONTEXT.md
@src/chair-view-redesign/DESIGN.md
@src/chair-view-redesign/code.html
@src/main/resources/static/chair.html

<interfaces>
From `src/main/resources/static/chair.html`:
```js
const elQueue = $('#queue');
const elQCount = $('#qCount');
const elCurName = $('#curName');
const elCurInitials = $('#curInitials');
const elLimit = $('#limit');

function updateUI(state) {
  // queue, current speaker, timers, poll, room config
}

$('#btnParticipantRequest').onclick = () => { /* request */ };
$('#btnParticipantWithdraw').onclick = () => { /* withdraw */ };
$('#btnNext').onclick = () => { /* next speaker */ };
$('#btnPause').onclick = () => { /* pause timer */ };
$('#btnStart').onclick = () => { /* start timer */ };
$('#btnReset').onclick = () => { /* reset timer */ };
$('#btnSetLimit').onclick = () => { /* update limit */ };
$('#btnPopout').onclick = () => { /* open utility popout */ };
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild the Meeting Controls card without changing the chair action contract</name>
  <files>src/main/resources/static/chair.html</files>
  <action>Recompose the left-column Meeting Controls card per D-01, D-02, and D-03 so the chair keeps the existing speaker-name proxy request/withdraw field, the per-speaker limit input, the Start/Pause/Next/Reset timer grid, and the popout utility inside the new Orchestrator shell. Preserve the current element IDs and send destinations exactly; this is a layout migration, not a protocol rewrite.</action>
  <verify>
    <automated>pwsh -NoProfile -Command "rg -n 'id=\"btnParticipantRequest\"|id=\"btnParticipantWithdraw\"|id=\"btnStart\"|id=\"btnPause\"|id=\"btnNext\"|id=\"btnReset\"|id=\"btnSetLimit\"|id=\"btnPopout\"|id=\"limit\"|id=\"participantName\"' src/main/resources/static/chair.html; if ($LASTEXITCODE -ne 0) { exit 1 }"</automated>
  </verify>
  <done>The Meeting Controls card exists in the redesigned left column and all existing chair control hooks remain addressable by the same DOM IDs.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild the Speaker Queue card as an operational right-column surface</name>
  <files>src/main/resources/static/chair.html</files>
  <action>Rework the right-column queue card per D-04 and D-05 so the current speaker is visually separated at the top, queued speakers are listed below in the order from the live room snapshot, and a visible Force Add Speaker action is exposed without introducing new queue data or backend payloads. Keep the queue rendering driven by `updateUI(state)` and preserve the existing current speaker and queue-count bindings.</action>
  <verify>
    <automated>pwsh -NoProfile -Command "rg -n 'id=\"section-queue\"|id=\"queue\"|id=\"qCount\"|Force Add Speaker|current speaker|queued speakers|curName|curInitials' src/main/resources/static/chair.html; if ($LASTEXITCODE -ne 0) { exit 1 }"</automated>
  </verify>
  <done>The queue reads as an operational status panel with the current speaker pinned apart from the waiting queue and a visible Force Add Speaker control.</done>
</task>

</tasks>

<verification>
Confirm the left-column Meeting Controls and right-column Speaker Queue are both migrated into the new shell while keeping the existing chair action IDs and STOMP destinations intact.
</verification>

<success_criteria>
The chair can operate speaker requests, timer controls, and queue management from the redesigned panels without any backend changes or routing changes.
</success_criteria>

<output>
After completion, create `.planning/phases/04-functional-control-migration/04-01-SUMMARY.md`
</output>