---
phase: 04-functional-control-migration
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/main/resources/static/chair.html
autonomous: false
requirements:
  - CTRL-05
  - CTRL-06
must_haves:
  truths:
    - "The Live Poll panel shows the active poll content and the latest results inside the new frosted glass treatment and still offers the end-poll action."
    - "Share Access exposes working copy-code and copy-link actions from the redesigned left column."
    - "The existing room metadata and configuration stay visible in the settings/menu area while the chair shell remains single-page and responsive."
  artifacts:
    - path: src/main/resources/static/chair.html
      provides: "Live Poll glass-panel card with active, results, creation, and empty states"
    - path: src/main/resources/static/chair.html
      provides: "Share Access card with copy-code and copy-link actions"
    - path: src/main/resources/static/chair.html
      provides: "Room metadata/configuration display retained in the menu/settings area"
  key_links:
    - from: "src/main/resources/static/chair.html"
      to: "updatePollUI(state)"
      via: "poll state machine renders active/results/empty views"
      pattern: "pollSelection|pollCreation|pollActive|pollResults|pollNone|btnEndPoll"
    - from: "src/main/resources/static/chair.html"
      to: "share.js"
      via: "existing copy/share handlers"
      pattern: "btnCopyCode|btnShareLink|btnShareParticipantUrl|copyStatus"
    - from: "src/main/resources/static/chair.html"
      to: "updateRoomConfigUI(state)"
      via: "room metadata rendering in the menu area"
      pattern: "configTopic|configParticipationFormat|configMeetingGoal|configDecisionRule|configDeliverable|configDescriptions"
---

<objective>
Finish the Phase 4 control migration by rebuilding the Live Poll and Share Access surfaces and confirming the settings/menu area still carries room metadata.

Purpose: The chair's left-column command surface must remain operational after the new layout lands, including poll control and share actions.
Output: Updated chair.html markup and wiring for the Live Poll card, Share Access card, and retained room-metadata display.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-functional-control-migration/04-CONTEXT.md
@src/chair-view-redesign/DESIGN.md
@src/chair-view-redesign/code.html
@src/main/resources/static/chair.html

<interfaces>
From `src/main/resources/static/chair.html`:
```js
const elPollSelection = $('#pollSelection');
const elPollCreation = $('#pollCreation');
const elPollActive = $('#pollActive');
const elPollResults = $('#pollResults');
const elPollNone = $('#pollNone');
const elRoomCodeLarge = $('#roomCodeLarge');
const elConfigDescriptions = $('#configDescriptions');

function updatePollUI(state) {
  // selection, creation, active, results, empty poll states
}

function updateRoomConfigUI(state) {
  // room metadata and description rendering
}

$('#btnStartPolling').onclick = () => { /* start poll */ };
$('#btnEndPoll').onclick = () => { /* end poll */ };
$('#btnCopyCode').onclick = async () => { /* copy room code */ };
$('#btnShareLink').onclick = async () => { /* copy participant link */ };
$('#btnShareParticipantUrl').onclick = async () => { /* share shortcut */ };
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild the Live Poll panel as the left-column frosted control surface</name>
  <files>src/main/resources/static/chair.html</files>
  <action>Recompose the Live Poll section per D-06 and D-07 so the poll creation, active poll, live results, and empty states render inside the new glass-panel treatment while keeping the existing poll STOMP handlers and DOM IDs. Preserve the End Poll action and make sure the panel still reads from `updatePollUI(state)` rather than introducing new poll data flows.</action>
  <verify>
    <automated>pwsh -NoProfile -Command "rg -n 'id=\"pollSelection\"|id=\"pollCreation\"|id=\"pollActive\"|id=\"pollResults\"|id=\"pollNone\"|id=\"btnEndPoll\"|id=\"btnStartPolling\"|id=\"btnStartMultiselectPolling\"' src/main/resources/static/chair.html; if ($LASTEXITCODE -ne 0) { exit 1 }"</automated>
  </verify>
  <done>The Live Poll panel is visually migrated, still shows active/results/empty states, and retains the end-poll control.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild Share Access and retain room metadata in the menu/settings area</name>
  <files>src/main/resources/static/chair.html</files>
  <action>Rework the Share Access card per D-08 and D-09, then keep the room metadata/configuration display in the settings/menu area per D-10. The copy-code and copy-link actions must continue to use the existing sharing logic, and the metadata UI must still be driven by `updateRoomConfigUI(state)` without adding a new route or separate settings page.</action>
  <verify>
    <automated>pwsh -NoProfile -Command "rg -n 'id=\"btnCopyCode\"|id=\"btnShareLink\"|id=\"btnShareParticipantUrl\"|id=\"roomCodeLarge\"|id=\"configTopic\"|id=\"configParticipationFormat\"|id=\"configMeetingGoal\"|id=\"configDecisionRule\"|id=\"configDeliverable\"|id=\"configDescriptions\"' src/main/resources/static/chair.html; if ($LASTEXITCODE -ne 0) { exit 1 }"</automated>
  </verify>
  <done>Share Access works from the redesigned left column and the room metadata remains visible in the menu/settings area.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify the full control migration in a browser</name>
  <files>src/main/resources/static/chair.html</files>
  <action>Pause after the automated poll and share-panel migration and have the user confirm the combined desktop/mobile chair experience in the browser.</action>
  <verify>Open the chair view at desktop and mobile widths, confirm the migrated panels render, and confirm the existing controls still work without page errors.</verify>
  <done>The browser check confirms the phase 4 control migration is visually and functionally acceptable.</done>
  <what-built>The Phase 4 chair migration now includes the rebuilt Meeting Controls, Speaker Queue, Live Poll, and Share Access panels inside the existing shell.</what-built>
  <how-to-verify>
1. Open the chair view in a browser at desktop width and confirm the left-column controls, right-column queue, and frosted poll/share panels render in the redesigned layout.
2. Exercise the visible actions: request/withdraw, timer buttons, end poll, copy code, and copy link. Confirm the existing behavior still fires without page errors.
3. Reduce the viewport to mobile width and confirm the single-page shell still works, the conference table remains hidden, and the menu/anchor navigation still reaches the migrated sections.
  </how-to-verify>
  <resume-signal>Type "approved" once the browser checks pass, or describe the issues that need correction.</resume-signal>
</task>

</tasks>

<verification>
Confirm the Live Poll, Share Access, and room metadata surfaces are migrated into the existing shell and then verify the full control migration in a browser.
</verification>

<success_criteria>
The chair can use the poll and sharing controls from the redesigned left column, and the existing room metadata stays visible in the menu/settings area without changing backend behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/04-functional-control-migration/04-02-SUMMARY.md`
</output>