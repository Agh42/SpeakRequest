---
phase: 05-integration-verification-completion
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - src/main/resources/static/chair.html
autonomous: false
requirements:
  - MIG-01
  - MIG-02
  - MIG-03
  - MIG-04
must_haves:
  truths:
    - "The migrated chair view still renders the live room state, room-destroyed, error, and chair-assumed flows correctly in the browser."
    - "The existing chair publish actions still operate from the redesigned controls without introducing new destinations or backend changes."
    - "Participant names and other user-supplied strings remain sanitized, and room metadata is still visible in the sidebar/settings area."
  artifacts:
    - path: src/main/resources/static/chair.html
      provides: "Browser-visible migrated chair UI with preserved live updates and actions"
    - path: src/main/resources/static/chair.html
      provides: "Sanitized render paths and retained room metadata display"
  key_links:
    - from: "src/main/resources/static/chair.html"
      to: "/topic/room/{roomCode}/state, /topic/room/{roomCode}/destroyed, /topic/room/{roomCode}/error, /topic/room/{roomCode}/chairAssumed, /user/queue/error"
      via: "browser-visible subscription flows"
      pattern: "client.subscribe\(`/topic/room/\\$\{roomCode\}/(state|destroyed|error|chairAssumed)`|/user/queue/error"
    - from: "src/main/resources/static/chair.html"
      to: "/app/room/{roomCode}/request, /withdraw, /next, /timer, /setLimit, /poll/start, /poll/end, /poll/close, /poll/cancel, /updateConfig, /destroy"
      via: "browser-visible chair action handlers"
      pattern: "client\.send\(`/app/room/\\$\{roomCode\}/(request|withdraw|next|timer|setLimit|poll/start|poll/end|poll/close|poll/cancel|updateConfig|destroy)`"
---

<objective>
Pause after the automated integration cleanup and have the browser experience checked end to end so the phase can close with high confidence.

Purpose: Phase 5 is the final proof that the redesign did not break the live meeting workflow.
Output: Human-confirmed browser verification of the migrated chair UI.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-integration-verification-completion/05-CONTEXT.md
@.planning/phases/05-integration-verification-completion/05-01-SUMMARY.md
@src/main/resources/static/chair.html

<interfaces>
From `src/main/resources/static/chair.html`:
```js
function updateUI(state) {
  // queue, current speaker, timers, poll, and config updates
}

function updateRoomConfigUI(state) {
  // room metadata stays visible in the sidebar/settings area
}

client.subscribe(`/topic/room/${roomCode}/state`, msg => { /* snapshot updates */ });
client.subscribe(`/topic/room/${roomCode}/destroyed`, msg => { /* destroyed-room updates */ });
client.subscribe(`/topic/room/${roomCode}/error`, msg => { /* error updates */ });
client.subscribe(`/topic/room/${roomCode}/chairAssumed`, msg => { /* chair-assumed updates */ });
client.subscribe(`/user/queue/error`, msg => { /* chair-only errors */ });
```
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify the migrated chair UI on desktop</name>
  <files>src/main/resources/static/chair.html</files>
  <action>Open the migrated chair view in a desktop browser and inspect the live bindings end to end before closing the phase.</action>
  <verify>Desktop browser check of the migrated chair UI.</verify>
  <what-built>The desktop chair view now uses the migrated Orchestrator shell with the existing live room-state, error, destroyed-room, and chair-assumed bindings.</what-built>
  <how-to-verify>
1. Open the chair view in a desktop browser and confirm the redesigned controls, queue, poll, and metadata areas render without console errors.
2. Trigger a few existing chair actions from the UI and confirm the current state updates still arrive and the page responds without navigation changes.
3. Confirm participant names, poll content, and room metadata still display correctly and do not render raw markup.
  </how-to-verify>
  <resume-signal>Type "approved" if the desktop browser checks pass, or describe the failures that need a follow-up fix.</resume-signal>
  <done>The desktop browser confirms the migrated chair UI still preserves the live meeting contract.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify the migrated chair UI on mobile and complete the phase</name>
  <files>src/main/resources/static/chair.html</files>
  <action>Repeat the browser smoke test at a mobile breakpoint and confirm the navigation and visibility rules still hold after the migration.</action>
  <verify>Mobile browser check of the migrated chair UI.</verify>
  <what-built>The mobile chair experience still uses the same live bindings while the conference-table area remains de-emphasized and the shell stays navigable.
  </what-built>
  <how-to-verify>
1. Reduce the browser width to a mobile breakpoint and confirm the conference table is hidden or de-emphasized as designed.
2. Confirm the mobile navigation or anchors still reach the migrated controls, queue, poll, and menu sections.
3. Re-check that the room metadata area remains available and that the existing chair actions still work from the redesigned controls.
  </how-to-verify>
  <resume-signal>Type "approved" once the mobile checks also pass, or describe any remaining issues.</resume-signal>
  <done>The mobile browser confirms the migrated chair UI is ready for phase completion.</done>
</task>

</tasks>

<verification>
Confirm the full browser experience still works after the integration cleanup and mobile layout checks.
</verification>

<success_criteria>
The migrated chair UI is visually and functionally acceptable on desktop and mobile, with all live bindings intact and no raw content rendering.
</success_criteria>

<output>
After completion, create `.planning/phases/05-integration-verification-completion/05-02-SUMMARY.md`
</output>