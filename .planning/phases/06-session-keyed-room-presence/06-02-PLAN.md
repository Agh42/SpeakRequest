---
phase: 06-session-keyed-room-presence
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - src/main/java/de/koderman/domain/Room.java
  - src/main/java/de/koderman/infrastructure/MeetingController.java
  - src/main/resources/static/chair.html
  - src/test/java/de/koderman/MeetingControllerPresenceTest.java
  - src/test/java/de/koderman/RoomMemberPresenceTest.java
autonomous: true
requirements:
  - PRES-01
  - PRES-02
  - PRES-03
  - PRES-04
gap_closure: true
must_haves:
  truths:
    - "Chair proxy requests from the same session create distinct room members instead of overwriting the chair avatar"
    - "Withdraw removes the chair-linked proxy member entries from room state as well as the queue entry"
    - "Normal same-session participant requests still replace the existing member entry"
    - "Chair avatar labels render with the requested truncation and placeholder rules from the snapshot state"
  artifacts:
    - path: "src/main/java/de/koderman/domain/Room.java"
      provides: "Session-aware member lifecycle for normal and chair-proxy presence"
      contains: "proxy member append and removal helpers"
    - path: "src/main/java/de/koderman/infrastructure/MeetingController.java"
      provides: "Request and withdraw routing for chair proxy presence"
      exports:
        - request
        - withdraw
    - path: "src/main/resources/static/chair.html"
      provides: "Chair avatar label rendering with bounded labels"
      contains: "8-character truncation and ?? fallback"
    - path: "src/test/java/de/koderman/MeetingControllerPresenceTest.java"
      provides: "Controller regression coverage for chair proxy member creation and removal"
      contains: "same session can create multiple proxy members"
    - path: "src/test/java/de/koderman/RoomMemberPresenceTest.java"
      provides: "Room regression coverage for replacement and removal behavior"
      contains: "session replacement"
  key_links:
    - from: "src/main/java/de/koderman/infrastructure/MeetingController.java"
      to: "src/main/java/de/koderman/domain/Room.java"
      via: "request and withdraw lifecycle branch for chair sessions"
      pattern: "isChairSession|upsertMember|removeMember"
    - from: "src/main/resources/static/chair.html"
      to: "/topic/room/{roomCode}/state"
      via: "existing snapshot-driven chair renderer"
      pattern: "/topic/room/${roomCode}/state"
---

<objective>
Close the remaining phase 6 gap so chair proxy requests can create and remove their own visible member entries without breaking the normal session-replacement behavior.

Purpose: The chair should be able to queue multiple on-behalf speakers from the same WebSocket session while the chair surface still renders from the existing room snapshot contract.
Output: Room lifecycle behavior for proxy members, updated chair rendering rules, and regression tests that cover the gap.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-session-keyed-room-presence/06-CONTEXT.md
@.planning/phases/06-session-keyed-room-presence/06-01-SUMMARY.md
@.planning/phases/06-session-keyed-room-presence/06-UAT.md

<interfaces>
From src/main/java/de/koderman/domain/Room.java:
```java
public State snapshot();
public void upsertMember(String sessionId, String name);
public void removeMember(String sessionId);
public void addParticipantToQueue(Participant participant);
public void withdrawParticipant(String name);
```

From src/main/java/de/koderman/infrastructure/MeetingController.java:
```java
@MessageMapping("/room/{roomCode}/request")
public void request(@DestinationVariable String roomCode, @Valid @Payload RequestSpeak msg, StompHeaderAccessor headerAccessor);

@MessageMapping("/room/{roomCode}/withdraw")
public void withdraw(@DestinationVariable String roomCode, @Valid @Payload Withdraw msg);
```

From src/main/resources/static/chair.html:
```javascript
function getParticipantInitials(name) {}
function getConferenceParticipants(state) {}
function renderConferenceTable(state) {}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Split chair proxy presence from normal session replacement</name>
  <files>src/main/java/de/koderman/domain/Room.java, src/main/java/de/koderman/infrastructure/MeetingController.java, src/test/java/de/koderman/MeetingControllerPresenceTest.java, src/test/java/de/koderman/RoomMemberPresenceTest.java</files>
  <behavior>
    - Normal participant requests from the same session still replace the existing member entry instead of duplicating presence.
    - Chair-session requests on behalf of participants can append distinct visible member entries from the same session.
    - Withdraw and disconnect cleanup remove the chair-linked proxy member entries before the next broadcast.
  </behavior>
  <action>Update the room aggregate so it can keep the ordinary session-keyed replacement path for participant self-requests while also supporting chair-proxy member appends/removals for the chair session. Branch the request handler on `isChairSession(sessionId)` so proxy requests add a distinct member record instead of overwriting the chair's own avatar, keep the existing queue mutation and snapshot broadcast flow intact, and make withdraw/disconnect cleanup remove the matching proxy member state. Extend the existing presence tests so they prove the normal replacement path still passes and the chair proxy path now produces multiple visible member entries for the same session.</action>
  <verify><automated>./gradlew.bat test --tests "de.koderman.RoomMemberPresenceTest" --tests "de.koderman.MeetingControllerPresenceTest"</automated></verify>
  <done>Room snapshots and controller lifecycle behavior distinguish normal replacement from chair-proxy appends, and the regression tests prove the gap is closed.</done>
</task>

<task type="auto">
  <name>Task 2: Tighten chair avatar label rendering</name>
  <files>src/main/resources/static/chair.html</files>
  <action>Update the avatar label helper so empty or unusable names render as `??`, visible labels are capped at 8 characters, and longer labels truncate with a single-character ellipsis. Keep the current snapshot-driven conference table, queue, and current-speaker rendering path unchanged so the UI still reads from `/topic/room/{roomCode}/state`.</action>
  <verify><automated>./gradlew.bat test --tests "de.koderman.RoomMemberPresenceTest" --tests "de.koderman.MeetingControllerPresenceTest"; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; if (-not (Select-String -Path "src/main/resources/static/chair.html" -Pattern "\?\?" -Quiet)) { exit 1 }; if (-not (Select-String -Path "src/main/resources/static/chair.html" -Pattern "slice\(0, 8\)|substring\(0, 8\)|…" -Quiet)) { exit 1 }</automated></verify>
  <done>The chair avatar helper shows fallback initials and bounded labels without changing the snapshot subscription or layout wiring.</done>
</task>

</tasks>

<verification>
The phase is closed when the chair proxy regression test passes, normal same-session replacement still passes, the chair-proxy withdraw path removes visible members, and the chair renderer uses the updated label rules without changing the existing STOMP snapshot flow.
</verification>

<success_criteria>
- Chair proxy requests from the same session no longer overwrite the chair's own visible member entry.
- Chair proxy withdraw removes the matching member entries from room state.
- Normal participant session replacement behavior remains intact.
- Chair avatar labels stay readable with the new placeholder and truncation rules.
</success_criteria>

<output>
After completion, create `.planning/phases/06-session-keyed-room-presence/06-02-SUMMARY.md`
</output>