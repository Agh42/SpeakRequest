---
phase: 10-avatar-name-truncation-and-chair-label-simplification
plan: 01
type: execute
wave: 1
depends_on:
  - 9
files_modified:
  - src/main/resources/static/chair.html
  - src/test/java/de/koderman/ChairSurfaceRequirementsTest.java
autonomous: false
requirements: []
must_haves:
  truths:
    - "Long avatar labels truncate to the first three visible characters plus ..."
    - "The chair bootstrap name is plain chair instead of Chair-Candidate"
    - "Existing chair surface rendering and DOMPurify sanitization remain intact"
  artifacts:
    - path: "src/main/resources/static/chair.html"
      provides: "Avatar label helper and chair bootstrap payloads"
      contains:
        - "getParticipantAvatarLabel(name)"
        - "chair"
    - path: "src/test/java/de/koderman/ChairSurfaceRequirementsTest.java"
      provides: "HTML-based label assertions"
      contains: "ann..."
  key_links:
    - from: "src/main/resources/static/chair.html"
      to: "src/test/java/de/koderman/ChairSurfaceRequirementsTest.java"
      via: "label behavior assertions"
      pattern: "ChairSurfaceRequirementsTest|getParticipantAvatarLabel|Chair-Candidate"
---

<objective>
Keep avatar names inside the conference-table circle by truncating long labels to a short prefix plus ellipsis, and replace the chair bootstrap name with a plain chair label.

Purpose: The chair surface should stay visually balanced when names are long, while the chair identity text becomes simpler and less awkward.
Output: Updated chair HTML plus regression tests that lock in the new label rules.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-avatar-name-truncation-and-chair-label-simplification/10-CONTEXT.md

<interfaces>
From src/main/resources/static/chair.html:
```javascript
function getParticipantAvatarLabel(name) {
  const cleaned = String(name || '').trim().replace(/\s+/g, ' ');
  if (!cleaned) return '??';
  if (cleaned.length <= 8) {
    return cleaned;
  }
  return `${cleaned.slice(0, 7)}…`;
}

client.send(`/app/room/${roomCode}/join`, {}, JSON.stringify({name:'Chair-Candidate'}));
client.send(`/app/room/${roomCode}/assumeChair`, {}, JSON.stringify({
  participantName: 'Chair-Candidate',
  requestId: 'chair-access-' + Date.now()
}));
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock in label rules with tests</name>
  <files>src/test/java/de/koderman/ChairSurfaceRequirementsTest.java</files>
  <behavior>
    - Long avatar labels are expected to render as a three-character prefix plus `...`.
    - The chair bootstrap payloads are expected to use `chair` instead of `Chair-Candidate`.
    - Existing chair surface checks for topic clamping, timer thresholds, and state wiring remain unchanged.
  </behavior>
  <action>Extend the existing chair surface test coverage so the new label truncation rule and chair-name simplification are asserted before the HTML is changed. Keep the assertions text-based and focused on the chair page source so the tests stay lightweight.</action>
  <verify><automated>./gradlew.bat test --tests "de.koderman.ChairSurfaceRequirementsTest"</automated></verify>
  <done>The chair surface test suite fails until the new label behavior is implemented and then passes once it is in place.</done>
</task>

<task type="auto">
  <name>Task 2: Update chair label rendering and chair bootstrap name</name>
  <files>src/main/resources/static/chair.html</files>
  <action>Change the avatar-label helper so names longer than the cutoff render as the first three visible characters plus `...`, and replace both hardcoded `Chair-Candidate` payloads with the plain `chair` name. Preserve DOMPurify usage, the current snapshot-driven rendering, and the rest of the chair-page behavior.</action>
  <verify><automated>./gradlew.bat test --tests "de.koderman.ChairSurfaceRequirementsTest" --tests "de.koderman.MeetingControllerPresenceTest"</automated></verify>
  <done>The chair page renders shortened avatar labels, the chair bootstrap name is plain `chair`, and the existing chair/presence regression checks still pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify chair label fit</name>
  <what-built>Avatar labels should remain inside the avatar circle with the new short-prefix truncation, and the chair identity should read simply as `chair`.</what-built>
  <action>Open the chair surface in a browser, confirm a long participant name like `annemarie` appears as `ann...` inside the avatar circle, and confirm the chair bootstrap no longer shows `Chair-Candidate` anywhere visible in the page source or rendered state.</action>
  <how-to-verify>
    1. Load a room with a long participant name in the chair view.
    2. Confirm the avatar text shows only the first three visible characters plus `...`.
    3. Confirm the chair bootstrap payloads use the plain `chair` label instead of `Chair-Candidate`.
  </how-to-verify>
  <resume-signal>Type "approved" or describe the mismatch you found.</resume-signal>
</task>

</tasks>

<verification>
The phase is complete when the avatar label truncation rule is asserted by tests, the chair bootstrap uses the plain `chair` name, and a human check confirms the labels fit inside the circle as intended.
</verification>

<success_criteria>
- Long avatar labels render as a short prefix plus `...`.
- The chair bootstrap uses the plain `chair` label.
- Existing chair surface behavior and sanitization remain intact.
- The new tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/10-avatar-name-truncation-and-chair-label-simplification/10-01-SUMMARY.md`
</output>
