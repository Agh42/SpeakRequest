---
phase: 11-participant-view-style-sync
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/resources/static/participant.html
autonomous: true
requirements:
  - PART-01
  - PART-02
must_haves:
  truths:
    - Participant surfaces use the chair-view visual language for the request card, room info, queue, poll box, and shared utility controls.
    - The participant queue uses the full circular avatar-table presentation style instead of the older plain list presentation.
    - Participant-only behavior remains intact, including request/withdraw actions, the existing poll box, and the poll overlay flow.
    - Chair-only controls stay out of participant.html, including start poll, destroy room, and chair menu actions.
  artifacts:
    - path: src/main/resources/static/participant.html
      provides: Participant page styling, queue rendering, and poll-box wiring
      contains:
        - class="card poll-box" id="pollSection"
        - queue-item
        - queue-avatar
        - poll-overlay .panel
        - Manrope
        - Inter
  key_links:
    - from: src/main/resources/static/participant.html:updateUI(state)
      to: src/main/resources/static/participant.html:updatePollUIParticipant(state)
      via: Shared state-driven participant rendering
      pattern: updateUI\(state\).*updatePollUIParticipant\(state\)
    - from: src/main/resources/static/participant.html:queue rendering
      to: src/main/resources/static/chair.html:renderConferenceTable(state)
      via: Shared circular avatar-table visual pattern
      pattern: conference-table-(shell|seat|avatar)
---

<objective>
Make participant.html visually match the redesigned chair surface for shared presentation elements while preserving participant-only actions and the existing poll-box behavior.

Purpose: Phase 11 closes the participant-view style gap without importing chair-only functions or new backend surfaces.
Output: A participant page that uses the same Orchestrator visual language, avatar-table presentation, and rounded button language as the chair view while staying read-only on chair controls.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-participant-view-style-sync/11-CONTEXT.md
@src/main/resources/static/chair.html
@src/main/resources/static/participant.html
@src/chair-view-redesign/DESIGN.md
@src/chair-view-redesign/code.html

<interfaces>
From src/main/resources/static/participant.html:
```javascript
function updateUI(state)
function updatePollUIParticipant(state)
function getParticipantInitials(name)
function getParticipantAccent(name)
function getReadableInkColor(hexColor)
```

From src/main/resources/static/chair.html:
```javascript
function renderConferenceTable(state)
function renderQueuePanel(state)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild participant surface language</name>
  <files>src/main/resources/static/participant.html</files>
  <read_first>
    - src/main/resources/static/participant.html
    - src/main/resources/static/chair.html
    - src/chair-view-redesign/DESIGN.md
    - src/chair-view-redesign/code.html
  </read_first>
  <action>
    Replace the older participant page styling with the chair-view visual language for shared surfaces: load Manrope and Inter, apply the dark radial Orchestrator background, convert the main cards and footer to glassy rounded panels, restyle the room code pills and button set with the chair-view rounded treatment, and add the circular avatar-table shell and avatar chip styling needed for the participant queue.

    Keep the participant page structurally participant-specific: preserve the existing request card, room info card, poll box, queue section, and poll overlay sections, but restyle them so they visually match the updated chair page without copying chair-only controls.
  </action>
  <acceptance_criteria>
    - src/main/resources/static/participant.html contains both Manrope and Inter font links.
    - src/main/resources/static/participant.html contains `class="card poll-box" id="pollSection"`.
    - src/main/resources/static/participant.html contains `.queue-item`, `.queue-avatar`, and `.poll-overlay .panel` styles.
    - src/main/resources/static/participant.html contains `room-code-big` styling.
    - src/main/resources/static/participant.html does not contain `btnStartPolling`.
    - src/main/resources/static/participant.html does not contain `btnDestroyRoom`.
  </acceptance_criteria>
  <verify>powershell -NoProfile -Command "Select-String -Path src/main/resources/static/participant.html -Pattern 'Manrope','Inter','card poll-box','queue-item','queue-avatar','poll-overlay .panel','room-code-big' | Out-Host; if (Select-String -Path src/main/resources/static/participant.html -Pattern 'btnStartPolling|btnDestroyRoom' -Quiet) { exit 1 }"</verify>
  <done>Participant.html uses the chair-view surface language for shared presentation elements and still excludes chair-only controls.</done>
</task>

<task type="auto">
  <name>Task 2: Wire participant queue and poll behavior to the new shell</name>
  <files>src/main/resources/static/participant.html</files>
  <read_first>
    - src/main/resources/static/participant.html
    - src/main/resources/static/chair.html
  </read_first>
  <action>
    Rewrite the participant queue rendering so each entry appears as a circular avatar chip with initials, the participant name, and a position badge inside the new avatar-table styling. Update the queue and poll transitions so `updateUI(state)` and `updatePollUIParticipant(state)` still drive the participant page, the existing request and withdraw handlers remain wired, and the poll box keeps its participant-only content while adopting the same tonal panel styling as the chair view.

    Do not add chair-only handlers or actions. Keep the participant page read-only with respect to chair controls: no start poll button, no destroy room button, no chair menu actions, and no chair polling launcher wiring.
  </action>
  <acceptance_criteria>
    - src/main/resources/static/participant.html contains `function updatePollUIParticipant(state)`.
    - src/main/resources/static/participant.html contains both `$('#btnRequest').onclick` and `$('#btnWithdraw').onclick`.
    - src/main/resources/static/participant.html contains `queue-item current` styling or markup for the highlighted participant row.
    - src/main/resources/static/participant.html uses `getParticipantInitials(name)` or `getParticipantAvatarLabel(name)` in queue avatar rendering.
    - src/main/resources/static/participant.html does not contain `btnStartPolling`.
    - src/main/resources/static/participant.html does not contain `conferenceTableTopic`.
  </acceptance_criteria>
  <verify>powershell -NoProfile -Command "Select-String -Path src/main/resources/static/participant.html -Pattern 'updatePollUIParticipant','btnRequest','btnWithdraw','queue-item current','getParticipantInitials','getParticipantAvatarLabel' | Out-Host; if (Select-String -Path src/main/resources/static/participant.html -Pattern 'btnStartPolling|conferenceTableTopic' -Quiet) { exit 1 }"</verify>
  <done>Participant queue and poll behavior still work, but the presentation now matches the chair-view shell and styling boundary.</done>
</task>

</tasks>

<verification>
Run the two task verification commands after implementation and confirm `participant.html` still has no chair-only controls while the participant queue and poll box render with the chair-view visual language.
</verification>

<success_criteria>
The participant page visually matches the redesigned chair surface for shared presentation elements, the queue uses the circular avatar-table style, and chair-only functionality remains absent from participant.html.
</success_criteria>

<output>
After completion, create `.planning/phases/11-participant-view-style-sync/11-01-SUMMARY.md`.
</output>