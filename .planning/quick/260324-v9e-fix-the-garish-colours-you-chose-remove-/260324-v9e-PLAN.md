---
phase: quick-260324-v9e
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/resources/static/chair.html
  - src/main/resources/static/styles.css
autonomous: true
requirements:
  - Q-260324-v9e
must_haves:
  truths:
    - "Chair view no longer uses decorative gradients for page/surface backgrounds and matches the redesign's calmer tonal surfaces."
    - "Desktop and mobile chair view preserve readability and hierarchy without introducing new bright or saturated accents."
    - "Poll type functionality named 'Gradients of Agreement' remains intact (feature wording/logic untouched); only visual gradients are removed."
  artifacts:
    - path: "src/main/resources/static/chair.html"
      provides: "Flat token-based background/surface classes replacing radial/linear gradient utility classes"
      contains: "No decorative radial-gradient or linear-gradient background utilities for shell/cards/orb"
    - path: "src/main/resources/static/styles.css"
      provides: "Non-gradient fallback styles aligned with redesign tokens"
      contains: "No linear-gradient declarations for base panel/button surfaces"
  key_links:
    - from: "src/chair-view-redesign/DESIGN.md"
      to: "src/main/resources/static/chair.html"
      via: "Surface and color token translation"
      pattern: "surface(_container|_container_high|_container_low)|on-surface"
    - from: "src/chair-view-redesign/code.html"
      to: "src/main/resources/static/chair.html"
      via: "Visual parity for major shells/cards"
      pattern: "bg-surface|bg-surface-container|text-on-surface"
---

<objective>
Remove the garish color treatment by eliminating decorative UI gradients introduced in the chair-view implementation and re-align the live chair styling to the redesign baseline.

Purpose: Restore the intended visual language from the redesign package (calm tonal layering, not flashy gradients).
Output: Updated chair markup and CSS with flat token-based surfaces, plus automated proof that forbidden gradient styles are gone.
</objective>

<execution_context>
@.github/get-shit-done/workflows/quick.md
@.github/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@src/chair-view-redesign/DESIGN.md
@src/chair-view-redesign/code.html
@src/chair-view-redesign/desktop.png
@src/chair-view-redesign/mobile.png
@src/main/resources/static/chair.html
@src/main/resources/static/styles.css
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Replace decorative gradients with redesign-consistent flat surfaces</name>
  <files>src/main/resources/static/chair.html, src/main/resources/static/styles.css</files>
  <action>In src/main/resources/static/chair.html, remove decorative radial/linear gradient utility classes from the chair shell, core cards, and central circle container; replace them with static token-backed surfaces from the redesign baseline in src/chair-view-redesign/code.html (e.g., surface, surface-container, surface-container-high, surface-container-low) while keeping existing structure, IDs, JS hooks, and behavior intact. In src/main/resources/static/styles.css, replace any linear-gradient declarations used for base panel/button appearance with flat color/token equivalents so the page no longer reads as neon/garish. Do not change poll feature names or poll-type logic (the 'Gradients of Agreement' functional concept is out of scope for this visual fix).</action>
  <verify>
    <automated>powershell -NoProfile -Command "$files = @('src/main/resources/static/chair.html','src/main/resources/static/styles.css'); $content = ($files | ForEach-Object { Get-Content $_ -Raw }) -join \"`n\"; if ($content -match 'radial-gradient\\(' -or $content -match 'linear-gradient\\(') { Write-Error 'Forbidden decorative gradients still present in chair visuals'; exit 1 }"</automated>
  </verify>
  <done>No decorative radial/linear gradient declarations remain in the chair page shell/surfaces, and visual tone matches the redesign's dark layered palette.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Guard against regression and verify redesign alignment</name>
  <files>src/main/resources/static/chair.html, src/main/resources/static/styles.css</files>
  <action>Run a targeted visual-alignment pass against src/chair-view-redesign/desktop.png and src/chair-view-redesign/mobile.png: confirm top bar, side/nav surfaces, cards, and central meeting area maintain subdued tonal contrast without bright gradients. Keep typography and spacing hierarchy intact. If any element appears over-saturated, tune only color/background classes in the two files above. Avoid touching backend Java code and avoid introducing new CSS variables or dependencies.</action>
  <verify>
    <automated>powershell -NoProfile -Command "$files = @('src/main/resources/static/chair.html','src/main/resources/static/styles.css'); $text = ($files | ForEach-Object { Get-Content $_ -Raw }) -join \"`n\"; $required = @('bg-surface','bg-surface-container','text-on-surface'); foreach ($token in $required) { if ($text -notmatch [regex]::Escape($token)) { Write-Error \"Expected redesign token missing: $token\"; exit 1 } }"</automated>
  </verify>
  <done>Chair visuals are calmer and consistent with redesign references across desktop/mobile while preserving existing interactive behavior.</done>
</task>

</tasks>

<verification>
Run both task automated checks; both must exit successfully.
</verification>

<success_criteria>
1. Chair shell/cards no longer use decorative radial/linear gradient backgrounds.
2. Color styling aligns with src/chair-view-redesign references (desktop and mobile screenshots) without introducing new vibrant gradients.
3. Existing chair behaviors remain intact because IDs, data flow, and JS handlers were not modified.
</success_criteria>

<output>
After completion, create .planning/quick/260324-v9e-fix-the-garish-colours-you-chose-remove-/260324-v9e-SUMMARY.md
</output>
